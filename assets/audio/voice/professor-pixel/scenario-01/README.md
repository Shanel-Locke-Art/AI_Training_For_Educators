# Professor Pixel Scenario 1 audio

Use `pp_s01_01.mp3` through `pp_s01_06.mp3` for the six approved production lines in the current Scenario 1 recording queue. The existing `pp_s01_00.mp3` is earlier runtime audio and must not be mistaken for one of the newly approved lines.

Confirm exact wording and status in `docs/asset-management/PromptCraft_Voice_Recording_Tracker.xlsx` before recording or replacing a file.

# Brand assets

Store approved PromptCraft and institutional brand files here. Runtime UI marks remain under `assets/images/ui/` when that path is part of the application contract. Add a file to the asset manifest before using it in the browser or printed output.
